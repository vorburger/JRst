<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ************************************************************* -->
    <!-- *** POM Relationships *************************************** -->
    <!-- ************************************************************* -->

    <parent>
        <groupId>org.nuiton</groupId>
        <artifactId>mavenpom</artifactId>
        <version>1.0.0-alpha-3-SNAPSHOT</version>
    </parent>

    <artifactId>maven-jrst-plugin</artifactId>
    <version>1.0.0-alpha-2-SNAPSHOT</version>

    <dependencies>
        <dependency>
            <groupId>org.nuiton</groupId>
            <artifactId>jrst</artifactId>
            <version>1.0.0-alpha-1</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.nuiton</groupId>
            <artifactId>maven-helper-plugin</artifactId>
            <version>${helper.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${maven.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-project</artifactId>
            <version>${maven.version}</version>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <!-- ************************************************************* -->
    <!-- *** Project Information ************************************* -->
    <!-- ************************************************************* -->
    <name>Maven JRST Plugin</name>
    <description>
        Maven2 plugin to generate reStructuredText file into xDoc.
    </description>
    <inceptionYear>2006</inceptionYear>
    <url>http://maven-site.nuiton.org/${project.artifactId}</url>
    <!--url>http://www.nuiton.org/embedded/${project.artifactId}</url-->

    <!-- ************************************************************* -->
    <!-- *** Build Settings ****************************************** -->
    <!-- ************************************************************* -->
    <packaging>maven-plugin</packaging>

    <properties>

        <!-- Site en et fr -->
        <maven.site.locales>fr,en</maven.site.locales>

    </properties>

    <build>

        <defaultGoal>install</defaultGoal>

        <plugins>

            <!-- plugin plugin -->
            <plugin>
                <artifactId>maven-plugin-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>helpmojo</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>

        <pluginManagement>
            <plugins>
                 <!--plugin>
                    <artifactId>maven-site-plugin</artifactId>
                    <dependencies>
                        <dependency>
                            <groupId>org.codelutin</groupId>
                            <artifactId>doxia-module-jrst</artifactId>
                            <version>1.0.1</version>
                        </dependency>
                    </dependencies>
                </plugin-->
            </plugins>
        </pluginManagement>

    </build>

    <!--Site-->
    <reporting>
        <plugins>
             <!--Site report's plugin-->
            <plugin>
                <artifactId>maven-plugin-plugin</artifactId>
            </plugin>
        </plugins>
    </reporting>

    <!-- ************************************************************* -->
    <!-- *** Build Environment  ************************************** -->
    <!-- ************************************************************* -->

    <!-- Source control management. -->
    <scm>
        <connection>scm:svn:svn://anonymous@labs.libre-entreprise.org/svnroot/jrst/maven-jrst-plugin/trunk</connection>
        <developerConnection>scm:svn:svn+ssh://tchemit@labs.libre-entreprise.org/svnroot/jrst/maven-jrst-plugin/trunk</developerConnection>
        <url>http://www.nuiton.org/repositories/browse/maven-jrst-plugin/trunk</url>

        <!-- a utiliser quand on sera sur nuiton -->
        <!-- ne fonctionnera pas pour les multi-modules a cause de l'expansion de l'artifactId
             il faudra alors remttre ces trois valeurs sur le pom parent du multi-module -->
        <!--connection>scm:svn:http://svn.nuiton.org/svn/${project.artifactId}/trunk</connection>
        <developerConnection>scm:svn:http://svn.nuiton.org/svn/${project.artifactId}/trunk</developerConnection>
        <url>http://www.nuiton.org/repositories/browse/${project.artifactId}/trunk</url-->
    </scm>

</project>
